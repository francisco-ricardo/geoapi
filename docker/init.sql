-- Habilita a extensão PostGIS para suporte a dados geoespaciais
CREATE EXTENSION IF NOT EXISTS postgis;

-- Habilita a extensão para tipos de geometria adicionais
CREATE EXTENSION IF NOT EXISTS postgis_topology;

